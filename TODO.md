# 待办列表

> 记录临时功能需求、bug 反馈和待办事项。完成后勾选。
> 与 DEVELOPMENT_PLAN.md 联动：若待办对应某个开发节点，标注 `→ 节点 XX`。

## 功能需求

- [x] 保存 AI 服务商配置后，弹出提示框显示保存成功或失败 → 节点 11
- [ ] 生成游戏时检测配置了几个模态的服务，提供选择：仅用配置的服务生成，或者使用本地示例资源替代缺失的模型内容

## Bug 修复

- [x] AI 服务商配置弹窗：左滑全选 API Key 输入框内容时，弹窗自动关闭（疑似点击空白区域触发关闭），需修复为输入框内操作不关闭弹窗 → 节点 11
- [x] 模型选择页面数据不对：DeepSeek 官网现只有 V4-FLASH 和 V4-PRO，下拉列表缺少 V4-FLASH，需更新模型列表数据

## 优化改进

- [x] 开发模式下保存的 API Key 在重新编译运行后应保留（只针对开发模式增加外部配置文件，该文件不提交到 Git）→ 节点 04
- [x] 创建游戏耗时较长（需要调用 AI 生成 GameScript），需增加优雅的加载动画效果，提升用户体验 → 节点 12
- [x] 记录模型调用历史（调用时间、模型、prompt、耗时、结果等），保存到本地文件，方便排查问题和分析成本
- [x] 各种生成任务（图片/视频/音乐/语音等）增加可视化任务管理窗口，统一查看当前任务列表、执行状态、失败/超时检测；确保存在重试机制和超时检测，异常时及时反馈到页面
- [x] 模型选择页面的模型数据（可选模型、接口地址、描述等）目前是否写死在代码中？如果是，抽取为独立配置文件（JSON），系统支持本地编辑和远程更新该配置文件
- [ ] 调研并梳理各 AI Provider 的接口兼容性情况：国内模型（DeepSeek/硅基流动/可灵等）接口格式是否统一为 OpenAI 兼容格式；各 Provider 的重试机制、超时检测、加解密签名逻辑是否完整；整理一份接口规范文档指导后续维护
- [ ] 创建游戏时生成任务应后台执行，用户退出创建页面不应打断正在进行的生成任务，需确保任务持久化并在后台继续运行
- [ ] 生成游戏页面，用户只输入文字时和用户输入markdown格式的文字时，两种情况下，点击生成游戏是否系统处理的不正确，markdown格式的会触发报错吗
